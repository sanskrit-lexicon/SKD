# Project Objective: SKD Cologne issue correction triage

## ➡️ Next Steps (Queue)

- [ ] Consider closing/commenting on SKD #13 only after the paired `csl-orig` and `csl-corrections` commits are pushed and maintainer comment-noise norms are satisfied.
- [ ] Re-triage SKD #1 separately: current `csl-orig/v02/skd/skd.txt` has no `dbika` matches, so the 2014 issue is stale or needs history-based closure, not a source patch.

## 🚧 Current Work-In-Progress (WIP)

- [ ] No active SKD source edit remains after the 2026-06-27 issue #13 batch.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- Candidate matrix, 2026-06-27:
  - SKD #13, `text-correction`/`minor`, source `../csl-orig/v02/skd/skd.txt`, evidence found for all 16 current bad lines, validation path `updateByLine.py` + temporary SKD `hw.py`/`make_xml.py`, decision `implemented`.
  - SKD #1, `text-correction`/`minor`, source `../csl-orig/v02/skd/skd.txt`, issue's original `dbika` pattern now absent, validation path not applicable, decision `answer-only/stale-current-source`.
- This Windows environment has Python and Mako but no `sh`/`bash`; for SKD validation, render pywork with `csl-pywork/v02/generate.py`, create an empty validation-only `hwextra/skd_hwextra.txt`, run `hw.py`, then require `make_xml.py` output `All records parsed by ET`.
- Local SKD worktree had a pre-existing untracked `prefaces/` directory; left untouched.

## ✅ Completed (Recent only)

- 2026-06-27: Implemented SKD #13 via `csl-orig/v02/skd/skd.txt` and audit-trailed it in `csl-corrections/batches/20260627/dictionaries/skd/change_skd_1.txt`.
- 2026-06-27: Validation passed: `updateByLine.py` applied 16 replacements, bad-pattern `rg` scan returned no matches, BOM bytes were `3c4c3e`, `hw.py` generated 42,531 headword rows, and `make_xml.py` printed `All records parsed by ET`.
- 08-07-2026: /cologne-pages-landing — SEO landing + .nojekyll live
